global:
  scrape_interval: 60s
  evaluation_interval: 60s
  scrape_timeout: 30s

scrape_configs:
  - job_name: 'data-lake'
    scrape_interval: 120s
    static_configs:
      - targets: ['app:8080']
        labels:
          service: 'data-lake'
          environment: 'development'
